#pragma once
#include "../Utilities/Math.h"

class Camera {
public: 
	/*Vector3 pos;
	Vector3 lookAt;
	Vector3 up = Vector3(0.0f, 1.0f, 0.0f);
	void treatingMovement();
	Matrix* ViewMatrix;
	Matrix* WorldMatrix;
	Vector3 xaxis;
	Vector3 yaxis;
	Vector3 zaxis;*/
};